CREATE TABLE in_order (
  id INTEGER,
  placed_order_id INTEGER,
  offer_id INTEGER,
  menu_item_id INTEGER,
  quantity INTEGER,
  item_price DECIMAL(12,2),
  price DECIMAL(12,2),
  comment VARCHAR(255)
)
